# OpenVINO Runtime + GenAI (PaddleOCR-VL inference)
openvino>=2025.4.0
openvino-genai>=2025.4.0
openvino-tokenizers>=2025.4.0

# Image / PDF
Pillow>=10.0.0
pypdfium2>=4.0.0           # 推荐：自带二进制，免装 poppler
pdf2image>=1.17.0          # 备用：需要 poppler 二进制
pdfplumber>=0.11.0

# RAG / embedding
chromadb>=0.5.0
huggingface_hub>=0.24.0       # 拉取 Qwen3 LLM / Embedding 的 OpenVINO IR
transformers>=4.54.0          # Qwen3 tokenizer + chat_template (enable_thinking=False)
jinja2>=3.1                   # apply_chat_template 必需（transformers 5.x 不再自带）
certifi>=2024.0               # macOS 系统 Python SSL 证书

# 可选：OCR 质量基线对比（src/inference.py TesseractBaseline），默认 demo 路径不依赖：
#   pip install pytesseract    # 另需系统安装 tesseract-ocr 二进制

# 工具
numpy>=1.26
tqdm>=4.66
rich>=13.7
